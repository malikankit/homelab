# Runtime state (data, configs, secrets) for self-hosted services lives
# outside this repo entirely, under ~/services/{state,configs}/ on the
# host — so there's normally nothing under services/ here to ignore.
